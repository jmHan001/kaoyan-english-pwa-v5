import{loadVocabulary,findWord}from'./vocabulary-manager.js';import{add}from'./learning-pool.js';const $=s=>document.querySelector(s);let selected='';await loadVocabulary();$('#input').value=localStorage.getItem('ky5_sentence')||'';function analyze(){const text=$('#input').value.trim();localStorage.setItem('ky5_sentence',text);const parts=text.split(/(?<=[,;:.!?])\s+|\s+(?=(?:although|because|while|which|that|who|when|if|but|and|therefore)\b)/i);$('#segments').innerHTML=parts.map((p,i)=>`<p><strong>${i===0?'主干候选':/although|while/i.test(p)?'让步或时间':/because|therefore/i.test(p)?'因果关系':/which|that|who/i.test(p)?'从句修饰':'补充成分'}</strong><br>${p.replace(/[A-Za-z]+(?:'[A-Za-z]+)?/g,w=>{const x=findWord(w);return`<span class="token ${x?.source||'unknown'}" data-word="${w}">${w}</span>`})}</p>`).join('')||'<div class="empty">请输入英文句子</div>'}$('#analyze').onclick=analyze;$('#sample').onclick=()=>{$('#input').value='Although technology has made information easier to obtain, the ability to judge which sources are relevant remains essential for students who wish to develop an independent perspective.';analyze()};document.addEventListener('click',e=>{const w=e.target.dataset.word;if(w){selected=w;const x=findWord(w);$('#mword').textContent=w;$('#mmeaning').textContent=x?`${x.translation} · ${x.source==='both'?'高考与考研共有':x.source==='gaokao'?'高考词':'考研词'}`:'当前词库未收录';$('#add').disabled=!x;$('#modal').classList.remove('hidden')}const lang=e.target.dataset.voice;if(lang){const u=new SpeechSynthesisUtterance(selected);u.lang=lang;speechSynthesis.cancel();speechSynthesis.speak(u)}});$('#close').onclick=()=>$('#modal').classList.add('hidden');$('#add').onclick=()=>{add(selected);$('#modal').classList.add('hidden')};if($('#input').value)analyze();
